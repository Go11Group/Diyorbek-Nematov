[request_defination]
r = sub, obj, act

[policy_defination]
p = sub, obj, act

[role_definition]
g = _, _

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = g(r.sub, p.sub) && keyMatch(r.obj == p.obj) && regexMatch(r.act == p.act)
